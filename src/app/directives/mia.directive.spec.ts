import { MiaDirective } from './mia.directive';

describe('MiaDirective', () => {
  it('should create an instance', () => {
    const directive = new MiaDirective();
    expect(directive).toBeTruthy();
  });
});
